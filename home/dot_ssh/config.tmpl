# SSH Configuration
# Note: SSH keys are managed by Bitwarden SSH agent.
# Ensure SSH_AUTH_SOCK is set correctly in your shell profile (e.g., dot_zshrc).

# Example for specific hosts if needed:
# Host github.com
#     HostName github.com
#     IdentityAgent ~/.bitwarden-ssh-agent.sock